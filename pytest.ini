[pytest]
testpaths = tests
pythonpath = src
markers =
    smoke: quick sanity checks
    boundary: edge case and boundary value tests
    unit: isolated tests for individual functions or components with no external dependencies
    integration: tests that validate interactions between two or more components or services
    system: end-to-end tests that validate complete workflows across the full stack

